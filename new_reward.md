这是一个非常专业且切中痛点的问题。在机器人连续控制（Continuous Control）和强化学习领域，仅仅依赖任务目标完成度和姿态模仿往往会导致策略学到高频抖动（Jitter）的不自然动作。

根据论文附录 C 中 Table 3 提供的定义，PianoMime 目前的 Song-specific 策略奖励函数完全由 **Key Press（按键准确度）** 和 **Mimic（指尖轨迹模仿）** 两个主要部分组成 。原论文提到的前置研究 RoboPianist 曾使用过包含能量惩罚（Energy Reward）的奖励函数设计来约束动作 ，但 PianoMime 在其最终公式中去除了这一项。为了提升手指与手臂运动的平滑度，我们需要在数学层面上引入显式的**平滑度惩罚（Smoothness Penalty）**。

以下是具体的计算方法与修改后的公式推导：

### 衡量运动平滑度的计算方法

要增强平滑度，我们需要量化“不平滑”的程度。在机器人运动学中，通常通过以下三个维度的惩罚项来衡量和约束运动的平滑度：

* 
**动作变化率（Action Rate Penalty）：** PianoMime 设定 Agent 的动作空间是 47 维的目标关节位置（Target Joint Positions）及其残差 。限制相邻两个时间步之间的网络动作输出差异，可以最直接地抑制高频抖动。
衡量指标为连续动作向量的 L2 范数平方：



$$S_{action} = ||a_t - a_{t-1}||_2^2$$


* **关节加速度 / 加加速度（Acceleration & Jerk Penalty）：** 在生物力学中，最常用于模拟人类自然运动平滑度的指标是最小化加加速度（Minimum Jerk，位置对时间的第三阶导数）。在离散时间 MDP 模型中，我们可以通过有限差分计算关节速度 $\dot{q}_t$ 和加速度 $\ddot{q}_t$：

$$S_{acc} = ||\ddot{q}_t||_2^2 \approx ||q_t - 2q_{t-1} + q_{t-2}||_2^2$$


* 
**关节扭矩 / 能量消耗（Torque / Energy Penalty）：** 剧烈且不平滑的运动必然导致底层 PD 控制器输出极大的瞬时扭矩 。约束控制力矩 $\tau$ 是一种隐性的平滑处理：



$$S_{torque} = ||\tau_t||_2^2$$



---

### 修改后 Reward Function 的具体形式

根据附录 C 的原始设计，当前的奖励函数结构为 ：


$$R_{original} = w_{task} \cdot r_{task} + w_{mimic} \cdot r_{mimic}$$


其中，权重各占 0.5。

为了与原论文将距离误差通过函数映射到 $[0, 1]$ 区间（例如文中提到的函数 $g(\cdot)$）的风格保持一致 ，我们可以通过高斯核函数将平滑度惩罚转化为正向的平滑奖励项 $r_{smooth}$。

**1. 定义平滑奖励项 $r_{smooth}$**
结合动作变化率与关节加速度，我们定义新的平滑子奖励项：


$$r_{smooth} = \exp\left( - \beta_1 ||a_t - a_{t-1}||_2^2 - \beta_2 ||\ddot{q}_t||_2^2 \right)$$


*(注：$\beta_1$ 和 $\beta_2$ 为调节敏感度的缩放超参数，用来控制惩罚的衰减速度。)*

**2. 组装最终的奖励函数**
将新的平滑项加入总奖励函数中，并重新分配权重：


$$R_{new} = w_1 \cdot \left[ 0.5 \cdot g(||s_k - k_g||_2) + 0.5 \cdot (1 - \text{false positive})^{2/3} \right] + w_2 \cdot \left[ g(||p_{df} - p_{rf}||_2)^{1/3} \right] + w_3 \cdot r_{smooth}$$

### 机制分析

PianoMime 的核心是残差策略（Residual Policy），即强化学习输出的是在 IK 逆运动学求解器基础上的残差项 $r_t$ 。IK 求解器生成的基准轨迹通常是相对平滑的。导致最终运动不平滑的根本原因，是 RL 策略为了去“够”那些边缘琴键或追求极端的按键准确率，在残差项上输出了剧烈的跳变。

引入上述公式中的 $||a_t - a_{t-1}||_2^2$ 惩罚项后，RL 优化器在逼近最优解时，会倾向于在更平滑的流形空间内寻找使 $r_{task}$ 和 $r_{mimic}$ 达到平衡的策略，从而在根源上解决高频 Jitter 问题。